{
  "name": "@terazi/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "run:tcmb": "tsx src/entrypoints/run-tcmb.ts",
    "run:tefas": "tsx src/entrypoints/run-tefas.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "@terazi/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@vitest/coverage-v8": "^2.1.8",
    "tsx": "^4.19.2",
    "typescript": "^5.6.0",
    "vitest": "^2.1.8"
  }
}
